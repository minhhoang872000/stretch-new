# Production build config (loaded automatically by `vite build`, mode=production).
# Public, non-secret — these get bundled into the client JS. Do NOT put secrets here.
VITE_API_BASE_URL=https://stretch-new.onrender.com/api/v1
VITE_SITE_URL=https://stretch.vn
